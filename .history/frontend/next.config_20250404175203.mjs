/** @type {import('next').NextConfig} */
const nextConfig = {};
module.exports = {
    images: {
      domains: ['localhost:3000'], // Add localhost to the list of allowed domains
    },
  };
export default nextConfig;
